Setup steps
  To install Cypress: npm install cypress --save-dev

How to run?
  To run Cypress: npx cypress open

Addition Installation:
If you are using xpath to get elements, you need to install @cypress/xpath
  To install XPATH in Cypress:  npm install -D @cypress/xpath
